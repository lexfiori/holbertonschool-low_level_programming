#include <stdio.h>

/**
 * main - Entry point
 * @argc: an integer
 * @argv: a pointer to a char argument
 *
 * Return: 0
 */

int main(int argc, char **argv)
{
	int i = 0;

	while (argc--)
	{
		
		i++;
	}
	return (0);
}
