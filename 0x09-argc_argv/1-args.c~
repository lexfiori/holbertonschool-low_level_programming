#include <stdio.h>

/**
 * main - Entry point
 * @argc: an integer
 * @argv: a pointer to a char argument
 *
 * Return - count
 */

int main(int argc, char **argv)
{
	int count = 0;

	while (argc--)
	{
		count++;
	}
	printf("%d\n", count);
	return (0);
}
