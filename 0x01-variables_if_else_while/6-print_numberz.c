#include<stdio.h>
/**
 * main - Entry point
 *
 * Description - this is a description
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		putchar (i);
	}

	putchar (10);
	return (0);
}
