#include<stdio.h>

/**
 * main - Entry point
 *
 * Description - this is a
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	for (c = 48; c <= 57; c++)
	{
		putchar (c);
			}
	for (c = 97; c <= 102; c++)
	{
		putchar (c);
			}
	putchar (10);
	return (0);
}
