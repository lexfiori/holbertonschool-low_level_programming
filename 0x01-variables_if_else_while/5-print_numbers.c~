#include <stdio.h>

/**
 * main - Entry point
 *
 * Description - this is a description
 *
 * Return: Always 0 (Success)
 */

 int main(void)
{
	int c;

	for (c = 0; c <= 9; c++)
	{
		printf("%i\n", c);
	}
	return (0);
}
