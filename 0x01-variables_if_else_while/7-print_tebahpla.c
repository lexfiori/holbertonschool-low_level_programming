#include <stdio.h>

/**
 * main - Entry point
 *
 * Description - this is a
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	for (c = 57; c >= 48; c--)
	{
		putchar (c);
	}
	putchar ('\n');
	return (0);
}
