#include<stdio.h>

/**
 * main - Entry point
 *
 * Description - this is a
 *
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c
		for (c = 48; c <= 57; c++)
		{
			if ( c != 57);
			{
				putchar (32);
				putchar (44);
			}
			putchar (c);
		}
	return (0);
}
